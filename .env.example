# Runtime configuration for the MCP server
# Path inside the container that the server will treat as the vault root.
# Ensure your deployment mounts the desired vault directory to this path.
VAULT_PATH=/vault

# Network configuration
HOST=0.0.0.0
PORT=8765
MCP_PATH=/mcp

# Optional: DNS rebinding protection
# When enabled, requests must match allowed hosts/origins
MCP_ENABLE_DNS_PROTECT=false
MCP_ALLOWED_HOSTS=
MCP_ALLOWED_ORIGINS=
